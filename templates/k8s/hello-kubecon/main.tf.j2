import {
  to = module.hello-kubecon.juju_model.model
  id = local.juju_model_name
}

module "hello-kubecon" {
  source = "github.com/terraform-modules.git//modules/hello-kubecon"

  juju_model_name = local.juju_model_name
  cloud_name      = local.cloud_name
}
