import {
  to = module.synapse.juju_model.model
  id = local.juju_model_name
}

module "synapse" {
  source = "github.com/terraform-modules.git//modules/k8s/synapse"

  juju_model_name = local.juju_model_name
  cloud_name      = local.cloud_name
}
